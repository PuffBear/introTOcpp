// a simple program working with variables 
#include <iostream>
using namespace std;
// so that we dont need to use std before every cout and cin

int main() {
    int x; // initialise a variable
    x = 4 + 2; // assign a value to the variable
    cout << x/3 << '&' << x*2;
    return 0;
}